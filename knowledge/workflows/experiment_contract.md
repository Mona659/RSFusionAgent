# 遥感融合实验契约

- 模拟实验使用目标时相原始 HS 作为低分辨率真值；MS/HS 输入按 3 倍空间尺度关系进行模糊下采样。
- 真实实验没有原生目标 HS 真值，系统仅可使用 HS 插值形成伪参考，指标必须标注为伪参考评估。
- TIFF 裁剪先生成可追溯的 `crop_manifest.json`，再按模型 Patch 选择进行推理。
- 默认 TIFF 检查采用已外部配准的显式像素对应声明：MS/HS 3×尺寸和裁剪窗口必须正确；小的 CRS/范围/分辨率元数据差异会警告而非阻断，系统不做自动配准或重投影。
- H5 演示模式直接复用已有 test Patch，不重复裁剪。
